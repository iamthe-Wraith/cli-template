{
  "name": "{{projectName}}",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "postinstall": "node ./bin/postinstall",
    "start": "webpack --config webpack.config.js --watch",
    "test": "mocha -r ts-node/register test/**/*.ts --watch --watch-extensions ts,tsx"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/{{author}}/{{projectName}}.git"
  },
  "author": "{{author}}",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/{{author}}/{{projectName}}/issues"
  },
  "homepage": "https://github.com/{{author}}/{{projectName}}#readme",
  "bin": {
    "{{projectName}}": "bin/{{projectName}}"
  },
  "dependencies": {},
  "devDependencies": {}
}
